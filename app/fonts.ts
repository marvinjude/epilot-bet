import { Instrument_Sans, Geologica } from "next/font/google";

export const instrumentSans = Instrument_Sans({
  subsets: ["latin"],
  display: "swap",
});

export const geologica = Geologica({
  subsets: ["latin"],
  display: "swap",
});
